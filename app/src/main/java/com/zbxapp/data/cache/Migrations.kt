package com.zbxapp.data.cache

import androidx.room.migration.Migration

/**
 * Room migrations registered on the database builder. The array is currently empty
 * because the schema is at version 1; add [Migration] objects here when the schema
 * is bumped so existing user data is preserved.
 */
val ALL_MIGRATIONS: Array<Migration> = arrayOf()
